# 项目记录

## 2026-06-02 重新迁移并推送 GitHub

按要求删除本地改坏的 `medical-admin-platform`，从 GitHub 重新克隆后重新迁移。

主要结果：

- 重新迁移后端、前端和可用项目资源。
- 后端包名统一为 `com.smartclinic`。
- 启动类统一为 `com.smartclinic.MedicalAdminApplication`。
- Maven 模块整理为医疗后台结构。
- 前端保留医疗运营后台页面并通过构建验证。
- 文档和忽略规则统一为医疗后台说明。

后端未启动，只执行编译验证，避免占用本地 IDEA 端口。
